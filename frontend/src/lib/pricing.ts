/** Single source of truth for public pricing copy + PayPal amount. */
export const PRICE_USD = "9.99";
export const PRICE_LABEL = `$${PRICE_USD}`;
export const PRICE_LINE = `${PRICE_LABEL} USD`;
export const PRICE_ONE_TIME = `${PRICE_LABEL} one-time`;
