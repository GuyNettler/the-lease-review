import Link from "next/link";
const checks = ["Monthly rent, due date, and accepted payment methods", "Security deposit amount and conditions for deductions", "Lease start and end dates", "Automatic renewal and notice deadlines", "Early termination rules and fees", "Utilities, parking, and other recurring charges", "Maintenance responsibilities and repair reporting", "Guest, roommate, pet, and subletting rules", "Move-in condition documentation and insurance requirements", "Dispute, notice, and applicable-law provisions"];
export default function LeaseChecklistPage() { return <main className="min-h-screen bg-primary-light px-6 py-14 text-left"><article className="mx-auto max-w-3xl rounded-2xl bg-white p-8 shadow"><Link href="/" className="font-semibold text-primary">← Home</Link><h1 className="mt-6 text-4xl font-extrabold">10 things to check before signing a lease</h1><p className="mt-4 text-slate-700">Use this checklist to start a conversation about your apartment lease. It is informational, not legal advice.</p><ol className="mt-7 space-y-4">{checks.map((item, index) => <li key={item} className="flex gap-4"><span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-primary font-bold text-white">{index + 1}</span><span className="pt-1 font-medium text-slate-800">{item}</span></li>)}</ol><Link href="/upload" className="mt-8 inline-block rounded-full bg-primary px-6 py-3 font-bold text-white">Review my lease for $19.99</Link></article></main>; }
