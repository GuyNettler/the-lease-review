import Link from "next/link";
import { guides } from "@/lib/guides";
export default function GuidesPage() { return <main className="min-h-screen bg-primary-light px-6 py-14 text-left"><section className="mx-auto max-w-4xl"><Link href="/" className="font-semibold text-primary">← Home</Link><h1 className="mt-6 text-4xl font-extrabold">Lease guides</h1><p className="mt-3 text-slate-700">Plain-English guides to common apartment lease topics.</p><div className="mt-8 grid gap-5 md:grid-cols-2">{guides.map((guide) => <article key={guide.slug} className="rounded-xl bg-white p-6 shadow"><p className="text-sm font-bold uppercase text-primary">{guide.category.replaceAll("-", " ")}</p><h2 className="mt-2 text-xl font-bold">{guide.title}</h2><p className="mt-2 text-slate-700">{guide.excerpt}</p><Link href={`/guides/${guide.slug}`} className="mt-4 inline-block font-bold text-primary">Read guide →</Link></article>)}</div></section></main>; }
