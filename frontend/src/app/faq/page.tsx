import type { Metadata } from "next";
import Link from "next/link";
const faqs = [{ q: "Is The Lease Review legal advice?", a: "No. It provides general, informational analysis and does not create an attorney-client relationship." }, { q: "What can I upload?", a: "Residential lease documents in PDF, DOC, or DOCX format." }, { q: "How much does a review cost?", a: "Each review costs $19.99 USD as a one-time payment." }, { q: "What happens to my file?", a: "Files are deleted after processing." }, { q: "Can I use this for any state?", a: "The review can help you understand common lease terms, but state and local rules vary. Consult a qualified attorney for legal advice." }];
export const metadata: Metadata = { title: "Frequently Asked Questions", description: "Answers about The Lease Review." };
export default function FAQPage() { const schema = { "@context": "https://schema.org", "@type": "FAQPage", mainEntity: faqs.map(({ q, a }) => ({ "@type": "Question", name: q, acceptedAnswer: { "@type": "Answer", text: a } })) }; return <main className="min-h-screen bg-primary-light px-6 py-14 text-left"><section className="mx-auto max-w-3xl"><Link href="/" className="font-semibold text-primary">← Home</Link><h1 className="mt-8 text-4xl font-extrabold">Frequently asked questions</h1><div className="mt-8 space-y-4">{faqs.map(({ q, a }) => <article key={q} className="rounded-xl bg-white p-6 shadow-sm"><h2 className="text-lg font-bold text-primary">{q}</h2><p className="mt-2 text-slate-700">{a}</p></article>)}</div></section><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }} /></main>; }
